/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package org.gradle.provider.simple.java.app;

import org.gradle.provider.simple.java.list.LinkedList;

import static org.gradle.provider.simple.java.utilities.StringUtils.join;
import static org.gradle.provider.simple.java.utilities.StringUtils.split;
import static org.gradle.provider.simple.java.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
